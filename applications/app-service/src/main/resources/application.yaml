spring:
  application:
    name: "BackMutants"
  profiles:
    active: "${APP_ENV}"
info:
  app:
    name: "${spring.application.name}"
    description: "Micro Service Validate Mutant"
    version: "1.0.0"
management:
  endpoints:
    web:
      base-path: "/"
aws:
  dynamodb:
    endpoint: "http://localhost:8000"
    threads: "10"
